# Dockerfile para el frontend (desarrollo)
FROM node:18-alpine

# Establecer directorio de trabajo
WORKDIR /app

# Copiar archivos de dependencias
COPY package*.json ./

# Instalar todas las dependencias
RUN npm ci

# Copiar código fuente
COPY . .

# Exponer puerto
EXPOSE 3000

# Comando por defecto (usar vite para desarrollo)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
